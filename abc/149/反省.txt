1　複数文字列の入力の場合はinput().split().
2  K-A-B>=0 が　A-B-K>=0になっていた。サンプルを確かめよう。
3  toolsからis_primeを用いて解決。O(N*log(N))
4　文字について,使用した/してないを表現する配列を用いて解決

1~3に15分　4に一時間()
パフォーマンスについては不明(もしRatedなら600~900くらい?)

寝過ごした
Unratedになったらしい